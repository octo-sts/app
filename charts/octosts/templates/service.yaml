apiVersion: v1
kind: Service
metadata:
  name: {{ include "octosts.fullname" . }}
  labels:
    {{- include "octosts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "octosts.selectorLabels" . | nindent 4 }}
